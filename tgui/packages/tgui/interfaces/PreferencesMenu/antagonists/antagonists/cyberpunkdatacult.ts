import { type Antagonist, Category } from '../base';

const CyberpunkDataCult: Antagonist = {
  key: 'cyberpunkdatacult',
  name: 'Data Cult',
  description: [
    'Harvest network data, convert followers, and spend data on sci-fi cult escalation.',
  ],
  category: Category.Roundstart,
  priority: 15,
};

export default CyberpunkDataCult;
